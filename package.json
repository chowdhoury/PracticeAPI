{
  "name": "student-portal",
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch moragachass.json --host 0.0.0.0 --port $PORT"
  },
  "dependencies": {
    "json-server": "^0.17.3"
  }
}